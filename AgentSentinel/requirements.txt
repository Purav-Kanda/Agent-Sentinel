# requirements.txt
# AgentSentinel dependencies
# Install with: pip install -r requirements.txt

# Splunk Python SDK – for running SPL searches in MetaAgentWatcher
splunk-sdk>=2.0.0

# HTTP client used by SplunkWriter to POST to HEC
requests>=2.31.0

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0

# MCP Server (AgentSentinel MCP tools for Claude / Splunk AI Assistant)
mcp>=1.0.0

# Rich terminal UI (demo video)
rich>=13.0.0

# Optional: SSE transport for MCP server over HTTP
# uvicorn>=0.29.0
# starlette>=0.37.0

# Optional: for running the full demo with a real LLM
# anthropic>=0.25.0       # Claude API
# openai>=1.0.0           # OpenAI API
